import React from 'react'

/* 
create a landing page
with admin login
with user login

no sign up, every user will have their own login created by the admin

firebase or mongoDB
*/

export default function index() {
  return (
    <div>
        
    </div>
  )
}
